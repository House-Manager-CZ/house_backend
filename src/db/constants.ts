enum DB_TABLES {
  USERS = 'users',
  HOUSES = 'houses',
  HOUSE_USERS = 'house_users',
  EVENTS = 'events',
}

enum DB_ERROR_CODES {
  UNIQUE_CONSTRAINT = '23505',
}

export { DB_TABLES, DB_ERROR_CODES };
